{#
/**
 * @file reach_through.html.twig
 * Default theme implementation to present Reach-through entry data.
 *
 * This template is used when viewing Reach-through entry pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_reach_through()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('reach_through') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
