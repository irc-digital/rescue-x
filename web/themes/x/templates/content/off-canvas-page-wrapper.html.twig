{#
/**
 * Overriddes the default off-cavnvas functionality. We are not using the system tray functionality and we decided
 * that we did not like the extra wrapper that Drupal foisted on us
#}
{% if children %}
  {{ children }}
{% endif %}
