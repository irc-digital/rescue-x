{% include '@subatoms/50-tier-2-mixins/00-tier-2-mixin.twig' with {
  tier_2_mixin_name: 'typography-text-standard',
  tier_2_mixin_details: '<p>This mixin outputs our default body text, which is medium (<code>m</code>) font-sized text on mobile devices and scales up to medium-large (<code>ml</code>) on larger devices. It is unlikely you will need to include this mixin, as it is include on the <code>body</code> tag already.</p>',
} only %}
