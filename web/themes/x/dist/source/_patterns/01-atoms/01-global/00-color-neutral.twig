<ul class="sg-colors">
  {% for name,color in color_neutral %}
    <li>
      <span class="sg-swatch" style="background: {{ color }};"></span>
      <div class="sg-label">RGB: {{ color }}</div>
      <div class="sg-label">SCSS: {{ name }}</div>
    </li>
  {% endfor %}
</ul>