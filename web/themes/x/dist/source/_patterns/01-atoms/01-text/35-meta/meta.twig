{% set rendered_meta_text %}
  {{ meta_text }}
{% endset %}
{% if rendered_meta_text | trim is not empty %}
  <div class="rpla-meta{% for meta_modifier in meta_modifiers %} rpla-meta--{{ meta_modifier }}{% endfor %}">{{ meta_prefix }}{{ rendered_meta_text }}</div>
{% endif %}