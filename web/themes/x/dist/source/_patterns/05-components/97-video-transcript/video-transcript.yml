video_transcript_text:
  join():
    - include():
        pattern: "atoms-paragraph"
        with:
          paragraph_modifiers:
            - 'body'
    - include():
        pattern: "atoms-paragraph"
        with:
          paragraph_modifiers:
            - 'body'
    - include():
        pattern: "atoms-paragraph"
        with:
          paragraph_modifiers:
            - 'body'
    - include():
        pattern: "atoms-paragraph"
        with:
          paragraph_modifiers:
            - 'body'