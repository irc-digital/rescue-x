{% for name,size in font_size %}
    <div style="font-size: {{ size }}; line-height: 1.5;">The <strong>{{ name }}</strong> font size is <strong>{{ size }}</strong> and looks like this.</div>
{% endfor %}
