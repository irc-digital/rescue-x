{% if not typography_mixin_name %}
  <h3>Typography mixins</h3>
  <p>Typography mixins help ensure our typography is as consistent as possible. You should <code>@include</code> them in your components. This list is presented here mostly for designers.</p>
  <p>A note to developers. If you are using a mixin that accepts parameters and you wish to use the default behavior, do not pass any value in - allow the mixin to handle the defaulting. This should, hopefully, allow us to make system-wide changes a little more easily.</p>
{% else %}
  {% if typography_mixin_details %}
    <div>{{ typography_mixin_details }}</div>
  {% endif %}
  <p><strong>Usage:</strong></p>
  <p>
    <code>@include {{ typography_mixin_name }};</code>
  </p>
  {% if typography_mixin_parameters %}
    <p>This mixin accepts arguments, as outlined below:</p>
    <table>
      <thead>
      <tr>
        <th style='padding-right:2rem;'>Parameter</th>
        <th style='text-align:left;padding-right:2rem;'>Purpose</th>
        <th style='text-align:left;'>Usage</th>
      </tr>
      </thead>
      <tbody>
      {% for param,message in typography_mixin_parameters %}
        <tr>
          <td style='padding-right:2rem;'>{{ param }}</td>
          <td style='padding-right:2rem;'>{{ message }}</td>
          <td><code>@include {{ typography_mixin_name }}{% if not loop.first %}('{{ param }}'){% endif %};</code></td>
        </tr>
      {% endfor %}
      </tbody>
    </table>
  {% endif %}
  <p><strong>Examples:</strong></p>
  {% if not typography_mixin_example_text %}
    {% set typography_mixin_example_text = 'The output from this mixin looks like this.' %}
  {% endif %}
  {% if typography_mixin_parameters %}
    {% for param,message in typography_mixin_parameters %}
      {% if not loop.first %}
        <p><ul>Using <code>{{ param }}</code></ul></p>
        <div class="xxx-{{ typography_mixin_name }}-{{ param }}">{{ typography_mixin_example_text }}</div>
      {% else %}
        <p><ul>Default usage</ul></p>
        <div class="xxx-{{ typography_mixin_name }}">{{ typography_mixin_example_text }}</div>
      {% endif %}
    {% endfor %}
  {% else %}
    <div class="xxx-{{ typography_mixin_name }}">{{ typography_mixin_example_text }}</div>
  {% endif %}
{% endif %}