{% include '@subatoms/50-typography-mixins/00-typography-mixin.twig' with {
  typography_mixin_name: 'typography-text-standard',
  typography_mixin_details: '<p>This mixin outputs our default body text, which is medium (<code>m</code>) font-sized text on mobile devices and scales up to medium-large (<code>ml</code>) on larger devices. It is unlikely you will need to include this mixin, as it is include on the <code>body</code> tag already.</p>',
} only %}
