{% for name,space in spacing %}
  <div>
    <div style="padding: {{ space }};background-color: lightblue; display: inline-block; border: 1px solid black;margin-bottom:2rem;">This is padded with our <code>{{ name }}</code> spacing (<code>{{ space }}</code>).</div>
  </div>
{% endfor %}
