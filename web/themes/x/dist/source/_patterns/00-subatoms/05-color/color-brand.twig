<ul class="sg-colors">
  {% for name,color in color_brand %}
    <li>
      <span class="sg-swatch" style="background: {{ color }};"></span>
      <div class="sg-label">RGB: {{ color }}</div>
      <div class="sg-label">SCSS: get-brand-color('{{ name }}')</div>
    </li>
  {% endfor %}
</ul>